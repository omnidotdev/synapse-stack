apiVersion: v1
kind: Service
metadata:
  name: {{ include "synapse.fullname" . }}-gateway
  labels:
    {{- include "synapse.labels" . | nindent 4 }}
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.gateway.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "synapse.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: gateway
